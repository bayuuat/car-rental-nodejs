{
  "name": "probable-garbanzo",
  "version": "1.0.0",
  "description": "- Mulai modifikasi file `server/index.js` apabila ingin membuat HTTP server.\r - Mulai modifikasi folder `public` apabila ingin memodifikasi HTML.",
  "main": "index.js",
  "scripts": {
    "start": "node server/index.js",
    "build": "tsc",
    "serve": "node dist/index.js",
    "dev": "concurrently \"tsc --watch\" \"nodemon server/index.ts\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@flydotio/dockerfile": "^0.4.10",
    "@types/express": "^4.17.20",
    "@types/node": "^20.8.7",
    "concurrently": "^8.2.2",
    "nodemon": "^3.0.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2"
  }
}
